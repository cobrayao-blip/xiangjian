services:
  xiangjian:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ../.env
    environment:
      NODE_ENV: production
      BASE_PATH: /xiangjian
      PORT: "3000"
    restart: unless-stopped
